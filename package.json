{
  "name": "luzfija-es",
  "version": "1.0.0",
  "description": "Comparador de tarifas eléctricas",
  "private": true,
  "scripts": {
    "sync:seo-docs": "node scripts/sync-seo-docs.mjs",
    "test": "npm run sync:seo-docs && vitest run",
    "test:watch": "npm run sync:seo-docs && vitest"
  },
  "devDependencies": {
    "jsdom": "^27.0.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "chart.js": "^4.5.1"
  }
}
